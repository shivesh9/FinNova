# API boundary

`BuildConfig.EXCHANGE_RATE_BASE_URL` configures the public demo exchange-rate service base. No credentials, account data, or money movement are sent remotely. Any failed response must be mapped to a domain error and cached Room rates remain available.
